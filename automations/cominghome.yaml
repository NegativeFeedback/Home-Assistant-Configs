  alias: 'Turn on lights when coming home'

  trigger:
   - platform: state
     entity_id: group.all_devices
     state: 'home'

  condition:
   - platform: state
     entity_id: group.all_devices
     state: 'home'
  #  - platform: state
  #    entity_id: sun.sun
  #    state: 'below_horizon'
  #  - platform: time
  #    after: '16:00'
  #    before: '00:00'

  action:
     service: switch.turn_on
     entity_id: switch.switchtest
